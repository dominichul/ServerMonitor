<nav class="navbar navbar-inverse navbar-static-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Server Checker", root_path, :class=> "navbar-brand text-uppercase"%>
    </div>
    
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navigation">
    <% if logged_in? %>
      <ul class="nav navbar-nav navbar-left">
        <li>
          <%= link_to root_path do %>
          <i class="fa fa-home"></i> &nbsp&nbsp Home
          <% end %>
        </li>
        <li>
          <%= link_to user_sites_path(current_user) do %>
          <i class="fa fa-map-pin"></i> &nbsp&nbsp Sites
          <% end %>
        </li>
      </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
      <% if !logged_in? %>
        <li><a href="#">About</a></li>
        <li><a href="#">Features</a></li>
        <li><%= link_to "Sign In", login_path, :method => :get %></li>
      <% else %>
        <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">Account
        <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li>
          <%= link_to edit_user_path(current_user) do %>
          <i class="fa fa-wrench"></i> &nbsp&nbspAccount Settings
          <% end %>
        </li>
          <li>
          <%= link_to logout_path, method: :delete  do %>
          <i class="fa fa-sign-out"></i> &nbsp&nbspLogout
          <% end %>
        </li>
        </ul>
      </li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>